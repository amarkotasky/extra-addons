version: '3'
services:
  zivame:
    build: .
    # ports:
    #  - "3007:3000"
    environment:
      - nodeVersion
    # deploy:
    #   replicas: 2 
    # depends_on:
    #   - nginx
    # container_name: "${pro_name}_app"           
  nginx:
    image: nginx
    ports:
      - "8900:80"
    volumes:
      - "./nginx/nginx.conf:/etc/nginx/nginx.conf"
      - "./nginx/www:/data/www"      
    # links:
    #   - zivame 
    container_name: "zivame_nginx"
